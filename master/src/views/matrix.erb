<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>SnowRabbit</title>
	<link rel="stylesheet" href="/css/bootstrap.css">
	<link rel="stylesheet" href="/css/main.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" crossorigin="anonymous"></script>
	<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
	<script src="/js/jquery.main.js"></script>
</head>
<body>

        <div id="wrapper">
                <main id="main">
                        <div class="container">
                                <div class="latency-holder">
<h2>Snowrabbit Matrix</h2>
                                        <table>
                                                <thead>
                                                        <tr>
                                                                <th class="corner">&nbsp;</th>
<%
  @probes_list.each do |probe|
%>
<th class="heading-<%= probe[:color] %>"><%= probe[:site] %></th>
<% end %>
                                                        </tr>
                                                </thead>
<tbody>
<%
  @probes_list.each do |probe|
%>
<tr>
<th class="heading-<%= probe[:color] %>"><%= probe[:site] %></th>

<% @probes_list.each do |p| %>
<%   if probe[:site] == p[:site] %>
<td class="empty-box">&nbsp;</td>
<%
    else
      ping_metric = @ping_table.where(source_site: probe[:site], dest_site: p[:site]).order(Sequel.desc(:timestamp)).limit(1)
      if ping_metric.first
%>
<td class="popup-trigger" data-target="/site_details?source_site=<%=p[:site] %>&dest_site=<%= probe[:site] %>"><%= ping_metric.first[:avg] %></td>
<%
      end
    end
  end
%>
</tr>

<%
  end
%>

                                                </tbody>
                                        </table>
                                </div>
                        </div>
                </main>
        </div>
</body>
</html>

