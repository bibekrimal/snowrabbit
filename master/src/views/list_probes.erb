<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>SnowRabbit</title>
	<link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="css/bootstrap.css">
	<link rel="stylesheet" href="css/main.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js" defer></script>
  <script>window.jQuery || document.write('<script src="js/jquery-3.3.1.min.js" defer><\/script>')</script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" crossorigin="anonymous" defer></script>
	<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous" defer></script>
	<script src="js/jquery.timeago.js" defer></script>
	<script src="js/jquery.main.js" defer></script>
</head>
<body>
	<div id="wrapper">
		<main id="main">
			<div class="container">
				<section class="py-3">
					<h3 class="h6 font-weight-bold text-center mb-3">Probes</h3>
					<table class="table table-sm small mb-0">
						<thead>
							<tr>
								<th class="bg-light border-light">Site</th>
								<th class="text-right bg-light border-light">Location</th>
								<th class="text-right bg-light border-light">Description</th>
								<th class="text-right bg-light border-light">IP</th>
								<th class="text-right bg-light border-light">Secret</th>
								<th class="text-right bg-light border-light">Last Seen</th>
								<th class="text-right bg-light border-light">UI Color</th>
								<th class="text-right bg-light border-light">Active</th>
							</tr>
						</thead>
						<tbody>

<% @probes.each do |p| %>
							<tr>
								<td><%= p[:site] %></td>
								<td class="text-right"><%= p[:location] %></td>
								<td class="text-right"><%= p[:description] %></td>
								<td class="text-right"><%= p[:ip] %></td>
								<td class="text-right"><%= p[:secret] %></td>
								<td class="text-right">
<% if p[:last_seen] %>
<%=   Time.now.to_i - p[:last_seen] %> seconds ago
<% else %>
Not seen
<% end %>
</td>
								<td class="text-right">
									<div class="custom-form">
										<select class="color-select">
<%  @colors.each do |color|
      if p[:color] == color
 %>
										<option value="maroon" class="select-<%= color %>" selected><span class="sr-only"><%= color %></span></option>
<% else %>
										<option value="maroon" class="select-<%= color %>"><span class="sr-only"><%= color %></span></option>
<%    end
    end
%>
										</select>
									</div>
								</td>
								<td class="text-right"><%= p[:active] %></td>
							</tr>




<% end %>
						</tbody>
					</table>
				</section>


				<section class="py-3">
					<h3 class="h6 font-weight-bold text-center mb-3">Unregistered Probes</h3>
                                        <table class="table table-sm small mb-0">
                                                <thead>
                                                        <tr>
                                                                <th class="bg-light border-light">Site</th>
                                                                <th class="text-right bg-light border-light">IP</th>
                                                                <th class="text-right bg-light border-light">Secret</th>
                                                                <th class="text-right bg-light border-light">Active</th>
                                                        </tr>
                                                </thead>
                                                <tbody>

<% @probes_unregistered.each do |p| %>
<FORM ACTION="/register_probe" METHOD="POST">
<INPUT TYPE="hidden" NAME="site" VALUE="<%= p[:site] %>">
<TR>
<TD ALIGN=right><%= p[:site] %></TD>
<TD ALIGN=right><%= p[:ip] %></TD>
<TD ALIGN=right><INPUT NAME="secret" VALUE="<%= [*('a'..'z'),*('0'..'9')].shuffle[0,32].join %>" SIZE=50></TD>
<TD ALIGN=right><INPUT TYPE="submit" VALUE="Register"></TD>
</TR>
</FORM>
<% end %>
						</tbody>
					</table>
				</section>

				<section class="py-3">
					<h3 class="h6 font-weight-bold text-center mb-3">Inactive Probes</h3>
					<table class="table table-sm small mb-0">
						<thead>
							<tr>
								<th class="bg-light border-light">Site</th>
								<th class="text-right bg-light border-light">IP</th>
								<th class="text-right bg-light border-light">Last Seen</th>
							</tr>
						</thead>
						<tbody>

<% @probes_inactive.each do |p| %>
<TR>
<TD ALIGN=right><%= p[:site] %></TD>
<TD ALIGN=right><%= p[:ip] %></TD>
<TD ALIGN=right>
<% if p[:last_seen] %>
<%=   Time.now.to_i - p[:last_seen] %> seconds ago
<% else %>
Not seen
<% end %>
</TD>
</TR>
<% end %>
						</tbody>
					</table>
				</section>
			</div>
		</main>
	</div>
</body>
</html>


